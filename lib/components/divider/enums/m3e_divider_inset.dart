/// How far a divider is indented from the edges.
enum M3EDividerInset {
  /// Spans the full cross-axis length.
  full,

  /// Indents the leading edge only.
  inset,

  /// Indents both edges equally.
  middle,
}
