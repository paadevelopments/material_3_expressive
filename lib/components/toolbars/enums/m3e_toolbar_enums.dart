/// The vertical size of a toolbar.
enum M3EToolbarSize {
  small,
  medium,
  large,
}

/// The corner shape family for a toolbar container.
enum M3EToolbarShapeFamily {
  round,
  square,
}

/// The vertical density of a toolbar.
enum M3EToolbarDensity {
  regular,
  compact,
}

/// The color emphasis of a toolbar container.
enum M3EToolbarVariant {
  surface,
  tonal,
  primary,
}
