/// Preferred placement of a tooltip relative to its child.
enum M3ETooltipPlacement {
  /// Centered above the target (plain default).
  above,

  /// Centered below the target (e.g. app bars).
  below,

  /// Below toward the leading edge.
  bottomStart,

  /// Below toward the trailing edge (rich default).
  bottomEnd,
}
