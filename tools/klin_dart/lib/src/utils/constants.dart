/// Shared names used by the widget-context checks.
class Constants {
  /// Widget type name.
  static const String widget = 'Widget';

  /// `build` method name.
  static const String build = 'build';

  /// `createState` method name.
  static const String createState = 'createState';

  /// `BuildContext` type name.
  static const String buildContext = 'BuildContext';

  /// `widgetRef` parameter name.
  static const String widgetRef = 'widgetRef';

  /// Path fragment that identifies Flutter libraries.
  static const String flutter = 'flutter';

  /// `State` type name.
  static const String state = 'State';

  /// `StatefulWidget` type name.
  static const String statefulWidget = 'StatefulWidget';

  /// `ConsumerStatefulWidget` type name.
  static const String consumerStatefulWidget = 'ConsumerStatefulWidget';

  /// `ConsumerState` type name.
  static const String consumerState = 'ConsumerState';

  /// Name fragment used for screen classes.
  static const String screen = 'Screen';
}
